# Host Monitoring Server 0.9.11

Each Host instance now has one renewable long-lived authorization code. The Server stores the code encrypted, permits an administrator to reveal or rotate it, and invalidates the paired Client identity on rotation so the Client must pair again.

Cancelled unpaired instances can be permanently removed, and the administrator Web follows the shared instance-list, detail, configuration, and log workspace behavior with immediate language switching.
