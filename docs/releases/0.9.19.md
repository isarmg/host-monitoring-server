# Host Monitoring Server 0.9.19

- Server Web 删除产品仓库内复制的 Foundation 内容块 CSS，只从锁定的 `@sarmg/admin-ui` 0.8.2 发布包导入公共样式。
- Foundation 验收现在拒绝重新创建产品内 `appearance` 副本，并验证发布包实际导出内容块样式。
- 服务端管理 Web 源码统一放在 `web/`，不再使用容易被误解为独立 Client 的 `clients/web/`。
- Host 的实例统计、硬件趋势、设备详情、配对与删除行为仍由 Host 产品代码负责。
