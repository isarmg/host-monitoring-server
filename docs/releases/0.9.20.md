# Host Monitoring Server 0.9.20

- 过期的配对激活事务现在返回专用的 `pairing_transaction_expired` 错误、HTTP 410 和对应 `request_id`，不再与普通请求错误混淆。
- 不存在的事务继续使用 `pairing_transaction_not_found`；Client 可据此严格核对本地 request ID，通用路由 404 不会再被当成事务墓碑。
- 管理 Web 同步 Foundation 0.8.3 的实例列表、统计和通知行为；数据库 schema 与 Host 配对协议版本保持不变。
