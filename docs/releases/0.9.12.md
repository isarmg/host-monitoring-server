# Host Monitoring Server 0.9.12

The Server and administrator Web now consume Foundation 0.7.11 directly from the locked Rust and npm packages. The duplicated Platform Router and Web Shell source snapshots have been removed.

Build metadata derives the exact Foundation revision from `Cargo.lock` and verifies it with a dedicated regression test, preventing runtime diagnostics from drifting from the dependency graph.
