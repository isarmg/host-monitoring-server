# Host Monitoring Server 0.9.15

- 强化主机创建、详情路由、删除和历史保留周期，防止失效主机状态残留。
- 扩展历史查询与清理合同，并补齐浏览器、存储和 retention 回归测试。
- 升级到 Foundation 0.8.2 的不可变 Rust revision 与 Web Release 包。
- 只接受 0.9.15 当前配置、协议、Schema 与发行身份，不兼容旧版本状态。
