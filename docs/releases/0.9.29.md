# host-monitoring-server 0.9.29

- 修复 Host Client 恢复原身份配对后，管理台详情页短暂显示“所选实例已不存在”的问题。详情链接现在使用配对前后稳定的邀请 ID，监控查询仍使用当前 Host ID。
- Windows 旧版 AMD 驱动未提供 ADLX LUID、DXGI 又返回一条静态别名时，管理台将互补的使用率、显存和温度读数放在一张显卡卡片中，并列出所有相关读数 ID。原始报告和服务端指标汇总不变。
- 仍可打开使用当前 Host ID 的旧详情链接。Sentinel 和 Sunshine 的配对流程不更换实例 ID，无需对应修改。
- 数据库结构身份继续为 `0.9.26` / Schema `7`；本次只提升 Server 应用发行版本。

## 验证

- Web 生产构建通过；Chromium 和 Firefox 的实例配对、显卡展示回归与浏览器验收通过。
